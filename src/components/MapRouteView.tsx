import React from "react";
import { View, Text } from "react-native";

// Placeholder for a map with route polyline
// Replace with react-native-maps or Mapbox GL implementation
export default function MapRouteView() {
  return (
    <View
      style={{
        height: 200,
        backgroundColor: "#eef",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>MapRouteView (Polyline here)</Text>
    </View>
  );
}
